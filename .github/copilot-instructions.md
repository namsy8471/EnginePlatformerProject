# Copilot Instructions

## Project Guidelines
- User prefers that every Vulkan code change includes explanatory comments and that responses always include an overall flowchart after changes. 
- When importing models, the engine should auto-detect whether a model is animated or static and route to the appropriate loader automatically rather than requiring manual selection.

## Memory
- User prefers that each code change be accompanied by an overall flowchart in the response.